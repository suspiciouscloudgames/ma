// Switch this back to true when the question and play test features should return.
export const QUESTION_FEATURES_ENABLED = true;

// Keep the Japanese translations in code, but hide/disable them for Korean-only workshops.
export const JAPANESE_LOCALE_ENABLED = false;
