'use client';
import { ChangeEvent,useEffect,useRef,useState } from 'react';
import Link from 'next/link';
import { useExhibitState,useSubmission,SubmissionStatus } from './workshop-client';
import { preparePhoto } from './photo-processing';
export default function UploadPage(){
 const inputRef=useRef<HTMLInputElement>(null);const [file,setFile]=useState<File|null>(null),[preview,setPreview]=useState<string|null>(null);const state=useExhibitState(),submission=useSubmission('photo'),tr=state.locale==='tr',en=state.locale==='en';
 useEffect(()=>()=>{if(preview)URL.revokeObjectURL(preview);},[preview]);
 function choose(e:ChangeEvent<HTMLInputElement>){const next=e.target.files?.[0]??null;setFile(next);setPreview(next?URL.createObjectURL(next):null);submission.reset();}
 function previewFallback(){const selected=file;if(selected)void preparePhoto(selected).then(photo=>{if(inputRef.current?.files?.[0]===selected)setPreview(URL.createObjectURL(photo.thumbnail));}).catch(()=>{});}
 async function send(){if(!file)return;if(await submission.submit({file})){setFile(null);setPreview(null);if(inputRef.current)inputRef.current.value='';}}
 return <main className="upload-page"><section className="upload-card"><input ref={inputRef} disabled={submission.busy||state.workshop_closed} className="visually-hidden" id="photo" type="file" accept="image/jpeg,image/png,image/webp,image/heic,image/heif" onChange={choose}/>{preview?<button className="preview-button" disabled={submission.busy||state.workshop_closed} onClick={()=>inputRef.current?.click()}><img src={preview} onError={previewFallback} alt={tr?"Seçilen fotoğraf":en?'Selected photo':'선택한 사진'}/><span>{tr?"Başka fotoğraf seç":en?'Choose another photo':'다른 사진 선택'}</span></button>:<label className="pick-button" htmlFor="photo"><span className="plus">＋</span>{tr?"Fotoğraf arşivinden seç":en?'Choose from photo library':'사진첩에서 선택'}</label>}<button className="send-button" onClick={send} disabled={!file||submission.busy||state.workshop_closed}>{tr?"Ekrana gönder":en?'Send to screen':'스크린으로 보내기'}</button><SubmissionStatus state={submission.state} en={en} tr={tr} onRetry={send}/><Link className="wall-link" href="/gallery">{tr?"Ortak fotoğraf albümünü görüntüle":en?'View shared photo album':'공동 사진첩 목록 보기'}</Link>{state.questions_enabled?<Link className="sentence-link" href="/question">{tr?"2. Soru sor":en?'2. Ask a question':'2. 질문하기'}</Link>:<button className="sentence-link disabled" disabled>{tr?"2. Soru sor":en?'2. Ask a question':'2. 질문하기'}</button>}{state.display_mode==='play'&&!state.workshop_closed?<Link className="path-link" href="/respond">{tr?"3. Soru seç":en?'3. Choose a question':'3. 질문 선택하기'}</Link>:<button className="path-link disabled" disabled>{tr?"3. Soru seç":en?'3. Choose a question':'3. 질문 선택하기'}</button>}</section></main>;
}
